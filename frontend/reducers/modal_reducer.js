import {merge} from 'lodash'
const modalReducer = (state = {}, action) => {
    Object.freeze(state)
    switch (action.) {
        case value:
            
            break;
    
        default:
            break;
    }
}


export default modalReducer